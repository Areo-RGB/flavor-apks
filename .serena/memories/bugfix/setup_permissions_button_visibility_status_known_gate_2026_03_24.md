Updated setup permissions button visibility to only render when permission status is known and denied. Added RaceSessionController fields permissionsStatusKnown + shouldShowPermissionsButton, marked status known on request/event/passive refresh, and updated setup UI to use shouldShowPermissionsButton. Added widget tests covering granted startup (button hidden) and denied startup (button shown). Verified with dart analyze and flutter tests for race_session_screen and race_session_controller.